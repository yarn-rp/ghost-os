.build/
.swiftpm/
*.xcodeproj/
*.xcworkspace/
DerivedData/
*.o
*.dylib
.DS_Store
local/

# Claude Code plugin (local development)
.claude-plugin/
agents/
skills/
# …but DO track the bundled skills resources the recorder seeds into
# every recording's .agent/ dir. These ship with flow42 itself.
!Sources/Flow42Core/Resources/skills/
!Sources/Flow42Core/Resources/skills/**

# Release artifacts
ghost-os-*.tar.gz

# Python
__pycache__/
*.pyc
*.pyo

# PyInstaller (if used)
vision-sidecar/build/
vision-sidecar/dist/
*.spec
